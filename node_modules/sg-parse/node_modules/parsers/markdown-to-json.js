var readPost = require( 'utils/readPost' ), md2json;

md2json = function ( files ) {
	var i, data = [];

	i = files.length;

	while ( i-- ) {
		data[i] = readPost( files[i] );
	}

	return data;
};

module.exports = md2json;
