module.exports = function ( data, helpers ) {
	var pages, i;

	pages = [];

	return data.content.meta.map( function ( item ) {
		item.data = helpers.mustache.render( data.templates.base, {
			title: item.title,
			content: data.content[ item.content ]
		});

		return item;
	});
};