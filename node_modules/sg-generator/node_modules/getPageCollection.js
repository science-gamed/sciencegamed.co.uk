var fs, getPageCollection;


fs = require( 'fs' );

getPageCollection = function ( page, data, partials ) {
	var template, generator;

	template = fs.readFileSync( page.template ).toString();
	generator = require( 'page-generators/' + page.generator );

	return generator( template, data, partials );
};

module.exports = getPageCollection;