var readPost = require( 'utils/readPost' ), readPosts;

readPosts = function ( files ) {
	var i, data = [];

	i = files.length;

	while ( i-- ) {
		data[i] = readPost( files[i] );
	}

	return data;
};

module.exports = readPosts;
