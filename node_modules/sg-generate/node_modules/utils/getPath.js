var getPostLocation, months, getPath;

months = require( 'utils/months' );

getPath = function ( post ) {
	var date, year, month;

	date = post.date;
	
	return date.year + '/' + months[ date.month ] + '/' + post.id + '.html';
};

module.exports = getPath;