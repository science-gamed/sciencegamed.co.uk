var blogIndex, mustache, months, getPostLocation;

mustache = require( 'mustache' );
months = require( 'months' );
getPostLocation = require( 'getPostLocation' );


blogIndex = function ( template, data, partials ) {
	var pages = [], i;

	pages = [
		{
			path: 'project/src/blog/index.html',
			data: mustache.render( template, data, partials )
		}
	];

	return pages;
};

module.exports = blogIndex;