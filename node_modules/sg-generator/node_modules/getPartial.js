var fs, getPartial;


fs = require( 'fs' );

getPartial = function ( partial, data ) {
	var template, generator;

	template = fs.readFileSync( partial.template ).toString();
	generator = require( 'partial-generators/' + partial.generator );

	return generator( template, data );
};

module.exports = getPartial;