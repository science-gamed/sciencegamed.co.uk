module.exports = function ( grunt ) {
	
	var read;

	read = function ( files ) {
		var data = {};


	};

	grunt.registerTask( 'generator', 'Generate static pages', function () {
		var fs, config, subtask, generate;

		grunt.config.requires( 'generator' );

		fs = require( 'fs' );
		generate = require( 'generate' );

		config = grunt.config( 'generator' );

		for ( subtask in config ) {
			generate( config[ subtask ], grunt );
		}
	});

};